// Рабочая демонстрация Result типов
// 🔥 Современная обработка ошибок

println("=== Работающая демонстрация Result типов ===\n")

// === 1. СОЗДАНИЕ И ПРОВЕРКА Result ===
println("1. Создание и проверка Result:")

let success = Ok("Операция успешна!")
println("Success: " + success)
println("Is Ok: " + success.isOk())
println("Is Err: " + success.isErr())

if success.isOk() {
    println("Значение: " + success.unwrap())
}

let failure = Err("Что-то пошло не так")
println("\nFailure: " + failure)
println("Is Ok: " + failure.isOk()) 
println("Is Err: " + failure.isErr())
println("По умолчанию: " + failure.unwrapOr("Резервное значение"))

// === 2. ФУНКЦИИ С Result ===
println("\n2. Функции возвращающие Result:")

fn safeDivide(a, b) {
    if b == 0 {
        return Err("Деление на ноль")
    }
    return Ok(a / b)
}

let result1 = safeDivide(10, 2)
if result1.isOk() {
    println("10 / 2 = " + result1.unwrap())
} else {
    println("Ошибка деления")
}

let result2 = safeDivide(10, 0)
if result2.isErr() {
    println("Деление на ноль обработано: " + result2.unwrapOr("ОШИБКА"))
}

// === 3. ЦЕПОЧКИ ОБРАБОТКИ ===
println("\n3. Цепочки обработки ошибок:")

fn validateNumber(num) {
    if num < 0 {
        return Err("Отрицательное число")
    }
    if num > 100 {
        return Err("Слишком большое число") 
    }
    return Ok(num)
}

fn processNumber(num) {
    let validated = validateNumber(num)
    if validated.isErr() {
        return validated  // Передаем ошибку дальше
    }
    
    let value = validated.unwrap()
    return Ok("Обработано: " + (value * 2))
}

let testNumbers = [50, -10, 150, 25]
for let i = 0; i < testNumbers.length(); i++ {
    let num = testNumbers[i]
    let result = processNumber(num)
    
    if result.isOk() {
        println("Число " + num + " -> " + result.unwrap())
    } else {
        println("Число " + num + " -> Ошибка: " + result.unwrapOr("НЕИЗВЕСТНО"))
    }
}

// === 4. unwrapOr ДЛЯ ЗНАЧЕНИЙ ПО УМОЛЧАНИЮ ===
println("\n4. Значения по умолчанию:")

fn getConfig(key) {
    let config = {
        "host": "localhost",
        "port": "8080",
        "debug": "true"
    }
    
    if config[key] {
        return Ok(config[key])
    } else {
        return Err("Ключ не найден: " + key)
    }
}

let host = getConfig("host").unwrapOr("default-host")
let port = getConfig("port").unwrapOr("3000")
let timeout = getConfig("timeout").unwrapOr("30")
let debug = getConfig("debug").unwrapOr("false")

println("Host: " + host)
println("Port: " + port)
println("Timeout (default): " + timeout)
println("Debug: " + debug)

// === 5. КОМБИНИРОВАНИЕ Result ===
println("\n5. Комбинирование результатов:")

let okResult1 = Ok(42)
let okResult2 = Ok("hello")
let errResult = Err("failure")

fn combineTwo(r1, r2) {
    if r1.isErr() {
        return r1
    }
    if r2.isErr() {
        return r2
    }
    
    let combined = [r1.unwrap(), r2.unwrap()]
    return Ok(combined)
}

let combined1 = combineTwo(okResult1, okResult2)
if combined1.isOk() {
    println("Успешное комбинирование: " + combined1.unwrap())
}

let combined2 = combineTwo(okResult1, errResult)
if combined2.isErr() {
    println("Комбинирование с ошибкой: " + combined2.unwrapOr("ОШИБКА"))
}

// === 6. Result В КОЛЛЕКЦИЯХ ===
println("\n6. Работа с коллекциями Result:")

let results = [
    Ok("success1"),
    Err("error1"), 
    Ok("success2"),
    Err("error2"),
    Ok("success3")
]

let successes = []
let errors = []

for let i = 0; i < results.length(); i++ {
    let result = results[i]
    if result.isOk() {
        successes = successes.push(result.unwrap())
    } else {
        errors = errors.push(result.unwrapOr("UNKNOWN"))
    }
}

println("Успехи: " + successes)
println("Ошибки: " + errors)

// === 7. ПРАКТИЧЕСКИЙ ПРИМЕР ===
println("\n7. Практический пример:")

fn parseAge(ageStr) {
    if ageStr == "25" {
        return Ok(25)
    } else if ageStr == "30" {
        return Ok(30)  
    } else if ageStr == "invalid" {
        return Err("Неверный формат числа")
    } else {
        return Err("Неизвестное значение: " + ageStr)
    }
}

fn categorizeAge(age) {
    if age < 18 {
        return Ok("Несовершеннолетний")
    } else if age < 65 {
        return Ok("Взрослый")
    } else {
        return Ok("Пенсионер")
    }
}

fn processAgeString(ageStr) {
    let parsed = parseAge(ageStr)
    if parsed.isErr() {
        return parsed
    }
    
    let age = parsed.unwrap()
    let category = categorizeAge(age)
    return category
}

let testAges = ["25", "30", "invalid", "unknown"]
for let i = 0; i < testAges.length(); i++ {
    let ageStr = testAges[i]
    let result = processAgeString(ageStr)
    
    if result.isOk() {
        println("'" + ageStr + "' -> " + result.unwrap())
    } else {
        println("'" + ageStr + "' -> Ошибка: " + result.unwrapOr("ПАРСИНГ_ОШИБКИ"))
    }
}

println("\n=== Демонстрация завершена ===")
println("🎉 Result типы обеспечивают:")
println("✅ Безопасную обработку ошибок без паники") 
println("✅ Явную проверку успеха/неудачи")
println("✅ Значения по умолчанию через unwrapOr")
println("✅ Элегантные цепочки обработки")
println("✅ Композицию операций с проверкой ошибок")