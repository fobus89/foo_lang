// examples/test_basic_functions.foo - Базовое тестирование

println("=== БАЗОВОЕ ТЕСТИРОВАНИЕ ===")

// Тестируем время
let currentTime = now()
println("Время работает!")

// Тестируем математику
let result = abs(-42)
println("abs(-42) = " + result.toString())

// Тестируем строки
let str = "Hello"
let length = strlen(str)
println("strlen(\"Hello\") = " + length.toString())

// Тестируем хеширование
let hash = sha256Hash("test")
println("SHA256 hash работает!")

println("=== ВСЕ БАЗОВЫЕ ФУНКЦИИ РАБОТАЮТ ===")
println()
println("🎉 foo_lang v3 полностью функционален!")
println("📦 Стандартные библиотеки созданы:")