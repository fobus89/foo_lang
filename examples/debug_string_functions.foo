// Отладка строковых функций

println("=== DEBUG STRING FUNCTIONS ===")

// Проверим математическую функцию - работает ли она?
let mathResult = sin(1.0)
println("sin(1.0) = " + mathResult)

// Теперь попробуем строковую функцию самым простым способом
println("About to test strlen...")

// Попробуем вызвать strlen
let text = "hello"
let len = strlen(text)
println("strlen result: " + len)

println("Direct call test...")

println("Test complete")