// Простейший тест compile-time цикла

macro simpleTest() {
    generate {
        $for i in 3 {
            println("Number ${i}")
        }
        
        println("After loop")
    }
}

println("=== Simple compile-time for test ===")
@simpleTest()