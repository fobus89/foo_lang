// Тестируем граничные случаи индексации
let arr = [1, 2, 3]

println("Тест успешных случаев:")
println("arr[0] = " + arr[0])
println("arr[2] = " + arr[2])

println("Тест ошибочных случаев:")

// Эти случаи должны вызывать ошибки
// arr[3] // индекс вне границ  
// arr[-1] // отрицательный индекс
// arr["string"] // строковый индекс для массива

let obj = { key: "value" }
println("obj['key'] = " + obj["key"])
println("obj['missing'] = " + obj["missing"]) // должен вернуть <nil>

println("Все основные тесты прошли!")