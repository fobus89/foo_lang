// Демонстрация системы каналов в foo_lang
print("=== ДЕМО: Каналы для коммуникации ===")

// === 1. Создание каналов ===
print("\n--- Создание каналов ---")

// Небуферизованный канал (синхронный)
let unbufferedCh = newChannel()
print("Небуферизованный канал создан")

// Буферизованный канал (асинхронный)
let bufferedCh = newChannel(3)
print("Буферизованный канал создан (буфер: 3)")

// Информация о каналах
print("Информация о небуферизованном канале: " + channelInfo(unbufferedCh))
print("Информация о буферизованном канале: " + channelInfo(bufferedCh))

// === 2. Отправка и получение данных ===
print("\n--- Буферизованный канал ---")

// Отправляем данные в буферизованный канал
send(bufferedCh, "Hello")
send(bufferedCh, "World")
send(bufferedCh, 42)

print("Отправлено 3 сообщения в буферизованный канал")
print("Длина канала: " + len(bufferedCh).toString())
print("Емкость канала: " + cap(bufferedCh).toString())

// Получаем данные из канала
let msg1 = receive(bufferedCh)
let msg2 = receive(bufferedCh)
let msg3 = receive(bufferedCh)

print("Получено: " + msg1 + ", " + msg2 + ", " + msg3.toString())
print("Длина канала после получения: " + len(bufferedCh).toString())

// === 3. Неблокирующее получение ===
print("\n--- Неблокирующее получение ---")

// Пытаемся получить из пустого канала
let result = tryReceive(bufferedCh)
if result == "no_value" {
    print("✅ Канал пуст - tryReceive корректно вернул 'no_value'")
} else {
    print("❌ Ошибка: получили значение из пустого канала")
}

// Отправляем и тут же пытаемся получить
send(bufferedCh, "test_message")
let quickResult = tryReceive(bufferedCh)
print("Быстрое получение: " + quickResult)

// === 4. Async + каналы ===
print("\n--- Асинхронная работа с каналами ---")

// Создаем канал для коммуникации между горутинами
let asyncCh = make("chan", 5)

// Функция-продюсер (отправляет данные)
fn producer(ch, name, count) {
    for let i = 1; i <= count; i++ {
        let message = name + "_item_" + i.toString()
        send(ch, message)
        print("📤 Продюсер " + name + " отправил: " + message)
        
        // Имитируем работу
        await sleep(50) // 50мс
    }
}

// Функция-консумер (получает данные)
fn consumer(ch, name, count) {
    for let i = 1; i <= count; i++ {
        let message = receive(ch)
        print("📥 Консумер " + name + " получил: " + message)
    }
}

// Запускаем продюсеров асинхронно
let prod1 = async producer(asyncCh, "A", 3)
let prod2 = async producer(asyncCh, "B", 2)

// Запускаем консумера асинхронно
let cons1 = async consumer(asyncCh, "Main", 5)

// Ждем завершения всех задач
await Promise.all(prod1, prod2, cons1)

print("✅ Все асинхронные задачи завершены")

// === 5. Закрытие каналов ===
print("\n--- Закрытие каналов ---")

let testCh = make("chan", 2)
send(testCh, "last_message")

print("Перед закрытием: " + channelInfo(testCh))
close(testCh)
print("После закрытия: " + channelInfo(testCh))

// Пытаемся получить из закрытого канала
let finalMsg = tryReceive(testCh)
if finalMsg != "no_value" {
    print("Получили из закрытого канала: " + finalMsg)
}

// === 6. Практический пример - пайплайн обработки ===
print("\n--- Пайплайн обработки данных ---")

let inputCh = make("chan", 3)
let outputCh = make("chan", 3)

// Функция обработки
fn processor(input, output) {
    let data = receive(input)
    let processed = "PROCESSED[" + data + "]"
    send(output, processed)
    return processed
}

// Отправляем данные
send(inputCh, "data1")
send(inputCh, "data2")
send(inputCh, "data3")

// Обрабатываем асинхронно
let task1 = async processor(inputCh, outputCh)
let task2 = async processor(inputCh, outputCh)
let task3 = async processor(inputCh, outputCh)

// Ждем обработки
await Promise.all(task1, task2, task3)

// Получаем результаты
print("Результаты обработки:")
let result1 = receive(outputCh)
let result2 = receive(outputCh)
let result3 = receive(outputCh)

print("- " + result1)
print("- " + result2)
print("- " + result3)

// === Итоги ===
print("\n=== Итоги ===")
print("✅ Каналы созданы и работают")
print("✅ Отправка и получение данных")
print("✅ Буферизация работает корректно")
print("✅ Неблокирующие операции")
print("✅ Интеграция с async/await")
print("✅ Закрытие каналов")
print("✅ Пайплайн обработки данных")

print("\n🎉 Демонстрация каналов завершена!")
print("📡 Каналы обеспечивают безопасную коммуникацию между горутинами")