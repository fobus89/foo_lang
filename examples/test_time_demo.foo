// Демонстрация работы с датой и временем в foo_lang
println("=== ДЕМОНСТРАЦИЯ РАБОТЫ С ДАТОЙ И ВРЕМЕНЕМ ===")

// 1. Текущее время
println("\n🕐 Текущее время:")
let currentTime = now()
println("Текущее время: " + timeFormat(currentTime, "datetime"))
println("Год: " + timeYear(currentTime).toString())
println("Месяц: " + timeMonth(currentTime).toString())  
println("День: " + timeDay(currentTime).toString())
println("Час: " + timeHour(currentTime).toString())
println("Минута: " + timeMinute(currentTime).toString())
println("Секунда: " + timeSecond(currentTime).toString())

// 2. Создание времени из Unix timestamp
println("\n📅 Создание времени из timestamp:")
let timestamp = 1609459200  // 2021-01-01 00:00:00 UTC
let specificTime = timeFromUnix(timestamp)
println("Время из timestamp " + timestamp.toString() + ": " + timeFormat(specificTime, "datetime"))

// 3. Форматирование времени
println("\n🎨 Форматирование времени:")
let demoTime = timeFromUnix(1640995200)  // 2022-01-01 00:00:00 UTC
println("Только дата: " + timeFormat(demoTime, "date"))
println("Только время: " + timeFormat(demoTime, "time"))
println("Дата и время: " + timeFormat(demoTime, "datetime"))
println("Пользовательский формат: " + timeFormat(demoTime, "YYYY-MM-DD HH:mm:ss"))
println("ISO 8601: " + timeFormat(demoTime, "iso8601"))

// 4. Арифметика с датами
println("\n➕ Арифметика с датами:")
let baseTime = timeFromUnix(1609459200)  // 2021-01-01 00:00:00 UTC
println("Базовое время: " + timeFormat(baseTime, "datetime"))

let plusDays = timeAddDays(baseTime, 15)
println("+ 15 дней: " + timeFormat(plusDays, "datetime"))

let plusMonths = timeAddMonths(baseTime, 3)
println("+ 3 месяца: " + timeFormat(plusMonths, "datetime"))

let plusYears = timeAddYears(baseTime, 2)
println("+ 2 года: " + timeFormat(plusYears, "datetime"))

let plusHours = timeAddHours(baseTime, 12)
println("+ 12 часов: " + timeFormat(plusHours, "datetime"))

let plusMinutes = timeAddMinutes(baseTime, 30)
println("+ 30 минут: " + timeFormat(plusMinutes, "datetime"))

let plusSeconds = timeAddSeconds(baseTime, 45)
println("+ 45 секунд: " + timeFormat(plusSeconds, "datetime"))

// 5. Разность между временами
println("\n⏱️  Разность между временами:")
let time1 = timeFromUnix(1609459200)  // 2021-01-01 00:00:00 UTC
let time2 = timeFromUnix(1609545600)  // 2021-01-02 00:00:00 UTC (день спустя)

println("Время 1: " + timeFormat(time1, "datetime"))
println("Время 2: " + timeFormat(time2, "datetime"))

let diffSeconds = timeDiff(time2, time1)
println("Разность в секундах: " + diffSeconds.toString())

let diffMinutes = timeDiffMinutes(time2, time1)
println("Разность в минутах: " + diffMinutes.toString())

let diffHours = timeDiffHours(time2, time1)
println("Разность в часах: " + diffHours.toString())

let diffDays = timeDiffDays(time2, time1)
println("Разность в днях: " + diffDays.toString())

// 6. Сравнение времен
println("\n🔍 Сравнение времен:")
let earlier = timeFromUnix(1609459200)   // 2021-01-01 00:00:00 UTC
let later = timeFromUnix(1609545600)     // 2021-01-02 00:00:00 UTC
let same = timeFromUnix(1609459200)      // 2021-01-01 00:00:00 UTC

println("Раннее время: " + timeFormat(earlier, "datetime"))
println("Позднее время: " + timeFormat(later, "datetime"))

let isBefore = timeBefore(earlier, later)
println("Раннее время < Позднее время: " + isBefore.toString())

let isAfter = timeAfter(later, earlier)
println("Позднее время > Раннее время: " + isAfter.toString())

let isEqual = timeEqual(earlier, same)
println("Раннее время == Такое же время: " + isEqual.toString())

// 7. Дни недели
println("\n📆 Дни недели:")
let monday = timeFromUnix(1609804800)    // 2021-01-04 00:00:00 UTC (понедельник)
let tuesday = timeFromUnix(1609891200)   // 2021-01-05 00:00:00 UTC (вторник)
let wednesday = timeFromUnix(1609977600) // 2021-01-06 00:00:00 UTC (среда)

println("Понедельник: день недели " + timeWeekday(monday).toString() + " (0=Воскресенье)")
println("Вторник: день недели " + timeWeekday(tuesday).toString())
println("Среда: день недели " + timeWeekday(wednesday).toString())

// 8. Unix timestamps
println("\n🔢 Unix timestamps:")
let testTime = timeFromUnix(1640995200)  // 2022-01-01 00:00:00 UTC
println("Время: " + timeFormat(testTime, "datetime"))

let unixTimestamp = timeUnix(testTime)
println("Unix timestamp: " + unixTimestamp.toString())

// Проверяем обратное преобразование
let roundTripTime = timeFromUnix(unixTimestamp)
println("Обратное преобразование: " + timeFormat(roundTripTime, "datetime"))

let isEqualRoundTrip = timeEqual(testTime, roundTripTime)
println("Времена равны после обратного преобразования: " + isEqualRoundTrip.toString())

// 9. Практический пример: расчет возраста
println("\n🎂 Практический пример - расчет возраста:")
let birthDate = timeFromUnix(946684800)  // 2000-01-01 00:00:00 UTC (день рождения)
let today = now()

println("Дата рождения: " + timeFormat(birthDate, "date"))
println("Сегодня: " + timeFormat(today, "date"))

let ageInDays = timeDiffDays(today, birthDate)
println("Возраст в днях: " + ageInDays.toString())

let ageInYears = ageInDays / 365.25  // Учитываем високосные годы
println("Возраст в годах (приблизительно): " + ageInYears.toString())

// 10. Создание напоминаний
println("\n⏰ Создание напоминаний:")
let reminderBase = now()
let reminder1Hour = timeAddHours(reminderBase, 1)
let reminder1Day = timeAddDays(reminderBase, 1)
let reminder1Week = timeAddDays(reminderBase, 7)

println("Сейчас: " + timeFormat(reminderBase, "datetime"))
println("Напоминание через час: " + timeFormat(reminder1Hour, "datetime"))
println("Напоминание завтра: " + timeFormat(reminder1Day, "datetime"))
println("Напоминание через неделю: " + timeFormat(reminder1Week, "datetime"))

println("\n✅ Демонстрация работы с временем завершена!")
println("foo_lang поддерживает все основные операции с датой и временем:")
println("- Создание времени (now, timeFromUnix, timeFromString)")
println("- Получение компонентов (год, месяц, день, час, минута, секунда)")
println("- Форматирование (различные форматы и пользовательские)")
println("- Арифметика (добавление дней, месяцев, лет, часов, минут, секунд)")
println("- Сравнение (before, after, equal)")
println("- Разности (в секундах, минутах, часах, днях)")
println("- Unix timestamps и обратные преобразования")
println("- Дни недели и практические применения")