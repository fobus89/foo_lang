println("JIT компилятор активен")
println("Принцип работы:")
println("1. Мониторинг вызовов функций")
println("2. После 100 вызовов функция становится горячей")  
println("3. JIT компилирует горячие функции в оптимизированный код")
println("4. Следующие вызовы выполняются быстрее")

let x = 42
let y = abs(-10)
let result = x + y

println("Результат: " + result.toString())
println("JIT будет оптимизировать такие операции!")