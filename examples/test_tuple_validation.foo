// Тест валидации Tuple типов

println("=== Тест валидации Tuple типов ===")

// Правильные Tuple
let validTuple: (string, int) = ["hello", 42]
println("validTuple: " + validTuple[0] + ", " + validTuple[1].toString())

// Сложный Tuple
let complexValid: (string, float, bool, int) = ["test", 3.14, true, 100]
println("complexValid length: " + complexValid.length().toString())

// Тест ошибки - неправильное количество элементов
let wrongSize: (string, int) = ["only_one"]

println("=== Этот код не должен выполниться ===")