// Демонстрация Type Aliases в foo_lang v2

println("=== 🎯 Type Aliases - Псевдонимы типов ===")

// 1. Основные псевдонимы для примитивных типов
println("\n1. 📝 Основные псевдонимы:")

type UserId = int
type UserName = string
type Score = float
type IsActive = bool

println("UserId alias создан для типа int")
println("UserName alias создан для типа string")
println("Score alias создан для типа float")
println("IsActive alias создан для типа bool")

// 2. Использование псевдонимов в переменных
println("\n2. 🔧 Использование в переменных:")

let userId: UserId = 12345
let userName: UserName = "Alice"
let userScore: Score = 85.5
let userActive: IsActive = true

println("userId: " + userId.toString())
println("userName: " + userName)
println("userScore: " + userScore.toString())
println("userActive: " + userActive.toString())

// 3. Псевдонимы для коллекций
println("\n3. 📚 Псевдонимы для коллекций:")

type UserList = array
type UserData = object

let users: UserList = ["Alice", "Bob", "Charlie"]
let profile: UserData = {
    name: "Alice",
    age: 30,
    email: "alice@example.com"
}

println("users length: " + users.length().toString())
println("profile name: " + profile.name)

// 4. Использование псевдонимов в функциях
println("\n4. ⚙️  Использование в функциях:")

fn getUserInfo(id: UserId, name: UserName) -> UserData {
    return {
        userId: id,
        userName: name,
        timestamp: "2025-01-12"
    }
}

let userInfo = getUserInfo(userId, userName)
println("Generated user info:")
println("- ID: " + userInfo.userId.toString())
println("- Name: " + userInfo.userName)
println("- Timestamp: " + userInfo.timestamp)

// 5. Цепочка псевдонимов (псевдоним псевдонима)
println("\n5. 🔗 Цепочка псевдонимов:")

type PrimaryKey = UserId
type EntityId = PrimaryKey

let entityId: EntityId = 999
println("entityId (через цепочку псевдонимов): " + entityId.toString())

// 6. Псевдонимы делают код более читаемым
println("\n6. 📖 Читаемость кода:")

fn calculateUserRank(score: Score, isActive: IsActive) -> string {
    if !isActive {
        return "Inactive"
    }
    
    if score >= 90.0 {
        return "Expert"
    } else if score >= 70.0 {
        return "Advanced"
    } else if score >= 50.0 {
        return "Intermediate"
    } else {
        return "Beginner"
    }
}

let rank = calculateUserRank(userScore, userActive)
println("User rank: " + rank)

// 7. Практический пример: система координат
println("\n7. 🎯 Практический пример:")

type Coordinate = float
type Distance = float

fn calculateDistance(x1: Coordinate, y1: Coordinate, x2: Coordinate, y2: Coordinate) -> Distance {
    let dx = x2 - x1
    let dy = y2 - y1
    return sqrt(dx * dx + dy * dy)
}

let distance = calculateDistance(0.0, 0.0, 3.0, 4.0)
println("Distance between points: " + distance.toString())

println("\n✅ Type Aliases демонстрация завершена!")
println("🎉 Псевдонимы типов делают код более выразительным и понятным!")